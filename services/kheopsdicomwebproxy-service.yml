apiVersion: v1
kind: Service
metadata:
  name: kheopsdicomwebproxy
  namespace: postgres-operator
  labels:
    app: kheopsdicomwebproxy
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: kheopsdicomwebproxy
