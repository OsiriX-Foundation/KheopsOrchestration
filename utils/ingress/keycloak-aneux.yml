apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: keycloak-aneux-ingressroute
  namespace: kheops-utils
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`login-aneux.unige.ch`) && PathPrefix(`/`)
      services:
        - name: keycloak
          port: 80
  tls:
    secretName: login-aneux.unige.ch-tls