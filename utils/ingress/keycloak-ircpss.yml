apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: keycloak-ircpss-ingressroute
  namespace: kheops-utils
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`login-ircpss.unige.ch`) && PathPrefix(`/`)
      services:
        - name: keycloak
          port: 80
  tls:
    secretName: login-ircpss.unige.ch-tls