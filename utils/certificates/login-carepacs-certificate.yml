apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: login-carepacs.unige.ch
  namespace: kheops-utils
spec:
  secretName: login-carepacs.unige.ch-tls # secret à créer
  issuerRef:
    name: letsencrypt-prod # ClusterIssuer à utiliser
    kind: ClusterIssuer
  dnsNames:
    - login-carepacs.unige.ch