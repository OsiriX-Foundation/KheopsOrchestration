apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ohif.unige.ch
  namespace: kheops-utils
spec:
  secretName: ohif.unige.ch-tls # secret à créer
  issuerRef:
    name: letsencrypt-prod # ClusterIssuer à utiliser
    kind: ClusterIssuer
  dnsNames:
    - ohif.unige.ch