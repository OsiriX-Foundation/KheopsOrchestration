apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: login-aneux.unige.ch
  namespace: kheops-utils
spec:
  secretName: login-aneux.unige.ch-tls # secret à créer
  issuerRef:
    name: letsencrypt-prod # ClusterIssuer à utiliser
    kind: ClusterIssuer
  dnsNames:
    - login-aneux.unige.ch