apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: login-alzheimer.unige.ch
  namespace: kheops-utils
spec:
  secretName: login-alzheimer.unige.ch-tls # secret à créer
  issuerRef:
    name: letsencrypt-prod # ClusterIssuer à utiliser
    kind: ClusterIssuer
  dnsNames:
    - login-alzheimer.unige.ch