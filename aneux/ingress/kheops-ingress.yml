apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: kheops-ingressroute
  namespace: kheops-aneux
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`aneux.unige.ch`) && PathPrefix(`/`)
      services:
        - name: kheopsreverseproxy
          port: 80
  tls:
    secretName: aneux.unige.ch-tls