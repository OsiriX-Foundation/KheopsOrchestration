apiVersion: apps/v1
kind: Deployment
metadata:
  name: kheopsui
  namespace: kheops-aneux
spec:
  selector:
    matchLabels:
      app: kheopsui
  replicas: 1
  template:
    metadata:
      labels:
        app: kheopsui
    spec:
      containers:
      - name: kheopsui
        image: osirixfoundation/kheops-ui:nikpap_unige_k8s
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: kheops-config